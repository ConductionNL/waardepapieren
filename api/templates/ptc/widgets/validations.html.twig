
{# if request['@id'] is defined and request['@id'] is not empty %}
    {% set validations = commonground_resource_list({'component': 'trc', 'type': 'tokens'}, {'resource': request['@id']})['hydra:member'] %}
{% if validations is defined and validations is not empty %}
<div class="content-background">
    <div class="content">
        <h2>{{ 'validations'|trans|capitalize }}</h2>
        <ul>
            {% for validation in validations %}
                <li><span>{{ validation.message }}</span></li>
            {% endfor %}
        </ul>
    </div>
</div>
{% endif %}
{% endif #}
